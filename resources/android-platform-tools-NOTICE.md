# Android Platform Tools runtime

Test cat installers prepare and bundle the official Android SDK Platform Tools for the target operating system. The runtime is downloaded from Google's official Android repository during the build and is used for Android device discovery and performance collection.

The downloaded archive includes Google's own `NOTICE.txt`, which is kept with the packaged runtime.
